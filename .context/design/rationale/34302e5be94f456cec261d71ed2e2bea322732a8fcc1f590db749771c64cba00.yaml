anchor: ac-137
combinatorial:
  reason: 'Derived from REQ-08''s explicit missing-resource behavior. Techniques: negative/error over resource state {nonexistent published book}. Kept separate because requesting a nonexistent book changes the outcome to an error page instead of a successful detail page. No covering array applied: the source specifies no independent input dimensions to combine; the risk-judged interaction strength would have been 2 because no money, auth, or data-loss factor applies.'
  strength: 2
deferred_bindings: 0
expected_how: 'expected_mode: authored'
profile_family: unspecified
surface_state: absent
technique: 'Derived from REQ-08''s explicit missing-resource behavior. Techniques: negative/error over resource state {nonexistent published book}. Kept separate because requesting a nonexistent book changes the outcome to an error page instead of a successful detail page. No covering array applied: the source specifies no independent input dimensions to combine; the risk-judged interaction strength would have been 2 because no money, auth, or data-loss factor applies.'
